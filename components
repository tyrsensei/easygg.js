Server
    games
    connected
    players
    instances

    updateGame(game)
    getGames()
    updateConnected()
    getConnected()
    updatePlayer(player)
    getPlayers()
    updateInstance(instance)
    getInstances()
Player
    socketId
    nickname
    instances

    updateInfo(info)
    joinIntance(instance)
    getInstances()
Game
    namespace
    name
    minPlayers
    maxPlayers
    instances

    udpateInfo(info)
    addInstance(intance)
    getInstances()
Instance
    roomId
    game
    players({socketId : {score: 0, ready: false}})
    minPlayers
    maxPlayers
    started = false

    addPlayer(player)
    getPlayers()
    isReady()
    isStarted()
    startGame()
